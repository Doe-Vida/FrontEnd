import 'package:flutter/material.dart';
import 'package:front_end/constants/app_images.dart';

class FitTestDialog extends StatelessWidget {
  const FitTestDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      actions: [
      ],
    );
  }
}